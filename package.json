{
  "name": "merge-test",
  "version": "0.0.0",
  "scripts": {
    "build": "rm -rf .next && mkdir .next && cp index.html .next/"
  },
  "devDependencies": {
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^23.0.8"
  }
}
