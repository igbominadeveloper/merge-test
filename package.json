{
  "name": "merge-test",
  "version": "2.0.1",
  "scripts": {
    "build": "rm -rf .next && mkdir .next && cp index.html .next/"
  },
  "devDependencies": {
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^23.0.8"
  }
}
